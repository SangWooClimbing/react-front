import { APP_LOGO_URL, APP_LOGO_TEXT } from '../constants';

// All mock data arrays (mockUsers, mockGyms, mockVideoPosts, mockNotifications, mockStoreItems, mockUserProfile)
// and related data generation/manipulation functions (generateMediaItems, simulateGymVisitAndUpdateStats, etc.)
// have been removed.

// The application will now rely on API calls or other data sources.

// Re-export constants if they are still needed from here, otherwise components should import from `../constants` directly.
export { APP_LOGO_URL, APP_LOGO_TEXT };
